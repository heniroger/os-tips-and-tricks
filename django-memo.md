# Django Memo

- Configure venv
```bash
$ cd /path/to/project_folder/
$ python3 -m venv .venv
$ . .venv/bin/activate
$ pip3 install requests
```

- Check configuration
```bash
$ pip freeze
$ deactivate
$ pip3 freeze
```
